{
  "name": "failing-project",
  "version": "1.0.0",
  "description": "A project that fails - has syntax errors and won't compile",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "build": "node -c index.js"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}
